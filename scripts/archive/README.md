# Archived scripts

This folder stores non-canonical or deprecated scripts kept for reference.

- Primary runnable flows are under:
  - `scripts/central/`
  - `scripts/edge/`
  - `scripts/shared/`

Use archived scripts only when troubleshooting historical behavior.
