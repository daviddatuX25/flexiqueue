# E13.2 Summary — Capacitor Scaffold

**Completed:** 2026-04-22
**Commit:** 01e87e5 (already committed from previous session)
**Build:** ✅ BUILD SUCCESSFUL (41s, 93 tasks after JDK 21 fix)
**APK:** ✅ `android/app/build/outputs/apk/debug/app-debug.apk` (6.3MB → 6.6MB with SDK 36)

## What was done

### Task 1: Capacitor installed + configured
- `@capacitor/core`, `@capacitor/cli`, `@capacitor/android` installed (devDependencies)
- `capacitor.config.ts` created at project root:
  - `appId: 'ph.edu.ispsc.flexiqueue'`
  - `appName: 'FlexiQueue'`
  - `webDir: 'public'` (Vite output)
  - `server.url: 'http://127.0.0.1:8000'` (WebView loads from localhost)
  - `server.cleartext: true` (allow HTTP for localhost)
  - `server.androidScheme: 'http'`
  - `android.allowNavigation: ['127.0.0.1']` (SECURITY: restrict to localhost only)
- `build:apk` script added to `package.json`: `npm run build && npx cap sync android && cd android && ./gradlew assembleDebug`

### Task 2: Android project scaffolded + APK built
- `npx cap add android` ran successfully → `android/` directory created
- `AndroidManifest.xml` updated with `android:usesCleartextTraffic="true"` ✅
- `AndroidManifest.xml` has `android.permission.INTERNET` ✅
- `.gitignore` updated with `android/.gradle/`, `android/app/build/`

## Deviations Fixed

### JDK Version Issue (Auto-fixed)
- **Problem:** Default JAVA_HOME pointed to JDK 25 (major version 69), but Gradle 8.14.3 and Capacitor Android require JDK 17+ (up to JDK 21 for Capacitor 8's sourceCompatibility JavaVersion.VERSION_21)
- **Fix:** Downloaded and installed JDK 21 to `C:/jdk21`. Set `JAVA_HOME=/c/jdk21` for builds
- **Files modified:** `android/local.properties` (java.home updated)

### Android SDK Platform Issue (Auto-fixed)
- **Problem:** Capacitor 8's dependencies (`androidx.core:core:1.17.0`) require compileSdk 36, but only SDK 34 was installed
- **Fix:** Installed Android SDK Platform 36 via `sdkmanager "platforms;android-36"` using PowerShell
- **Files modified:** `android/variables.gradle` (already had compileSdkVersion=36)

## APK Verification
```
$ ls -la android/app/build/outputs/apk/debug/app-debug.apk
-rw-r--r-- 1 sarmi 197610 6,619,586 Apr 22 12:31 app-debug.apk
```
APK is 6.6MB - within expected 5-15MB range.

## Threat Model Compliance
- T-E13.2-01 (Information Disclosure): ✅ Mitigated via `allowNavigation: ['127.0.0.1']`
- T-E13.2-02 (Tampering): ✅ Mitigated via `allowNavigation` restricting WebView to localhost only

## Unblocked
- E13.4 — First-run bundle extraction plugin (needs `android/` directory)
- E13.5 — Foreground service + wake lock (needs `android/` directory)
